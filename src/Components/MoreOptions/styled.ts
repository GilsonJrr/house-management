import styled from "styled-components";

export const Container = styled.div`
  position: relative;
`;

export const OptionsWarper = styled.div`
  position: absolute;
  background-color: #ffffff;
  padding: 10px;
  border-radius: 10px;
  right: 0;
  white-space: nowrap;
`;
